import React from 'react';
import AdminLogin from './AdminLogin';

/** Kept for backwards compatibility; /admin now uses AdminLogin directly. */
const AdminRedirect = () => <AdminLogin />;

export default AdminRedirect;
