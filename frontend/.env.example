# Strapi API URL – set when your Strapi is deployed (e.g. admin.deltasoft.website).
# When unset, the app uses the proxy (localhost:1337).
REACT_APP_STRAPI_URL=https://admin.deltasoft.website
# News API path – your Strapi uses /api/newss.
REACT_APP_STRAPI_NEWS_API=newss
# Optional: Strapi Worker role ID (e.g. 2). If set, workers are detected by role id when /users/me is not available.
# REACT_APP_STRAPI_WORKER_ROLE_ID=2
