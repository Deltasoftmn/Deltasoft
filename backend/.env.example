# Server
HOST=0.0.0.0
PORT=1337

# App keys (Strapi generates these on first run if missing)
# APP_KEYS=key1,key2
# API_TOKEN_SALT=
# ADMIN_JWT_SECRET=
# TRANSFER_TOKEN_SALT=

# CORS: allow your frontend (e.g. Vercel) to call the API
# FRONTEND_URL=https://your-site.vercel.app
# Or comma-separated: CORS_ORIGINS=https://site1.vercel.app,https://deltasoft.website

# Database (default: SQLite)
# DATABASE_FILENAME=.tmp/data.db

# For PostgreSQL, uncomment and set:
# DATABASE_CLIENT=postgres
# HOST=127.0.0.1
# PORT=5432
# DATABASE=deltasoft
# USERNAME=postgres
# PASSWORD=
# SSL=false
